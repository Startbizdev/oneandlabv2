/** Illustrations empty state — listes rendez-vous et demandes infirmier. */
export const EMPTY_RDV_IMAGE = require('../../assets/empty-rdv.png');
export const EMPTY_DEMANDE_IMAGE = require('../../assets/empty-demande.png');

export const EMPTY_RDV_IMAGE_WIDTH = 188;
export const EMPTY_RDV_IMAGE_HEIGHT = Math.round(EMPTY_RDV_IMAGE_WIDTH * (266 / 277));

export const EMPTY_DEMANDE_IMAGE_WIDTH = 172;
export const EMPTY_DEMANDE_IMAGE_HEIGHT = Math.round(EMPTY_DEMANDE_IMAGE_WIDTH * (205 / 182));
