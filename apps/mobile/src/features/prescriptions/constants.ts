/** Onglet / menu « Prescriptions » — pro et infirmier. */
export const SHOW_PRESCRIPTIONS_TAB_NAV = true;

/** RDV chargés par page dans le sélecteur « Liée au RDV » (infinite scroll). */
export const PRESCRIPTION_APPOINTMENT_PAGE_SIZE = 12;

/** Patients chargés par page dans le sélecteur ordonnances (infinite scroll). */
export const PRESCRIPTION_PATIENT_PAGE_SIZE = 50;
