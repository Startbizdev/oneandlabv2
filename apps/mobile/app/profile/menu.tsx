import { ProfileHubScreen } from '@/features/profile/screens/ProfileHubScreen';

/** Menu compte (documents, zones, navigation) — accessible si besoin hors onglet Plus. */
export default function ProfileMenuRoute() {
  return <ProfileHubScreen />;
}
