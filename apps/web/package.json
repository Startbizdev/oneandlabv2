{
  "name": "@oneandlab/web",
  "version": "1.0.0",
  "private": true,
  "description": "Wrapper monorepo — code Nuxt dans ../../frontend",
  "scripts": {
    "dev": "npm run dev --prefix ../../frontend",
    "build": "npm run build --prefix ../../frontend",
    "generate": "npm run generate --prefix ../../frontend",
    "preview": "npm run preview --prefix ../../frontend"
  }
}
