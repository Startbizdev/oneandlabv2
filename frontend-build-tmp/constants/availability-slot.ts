/**
 * Écart minimum (heures) entre début et fin du créneau « disponibilité personnalisée » (double slider).
 * Utilisé sur /rendez-vous/nouveau et formulaires pro (lab, infirmier, admin, etc.).
 */
export const AVAILABILITY_MIN_SPAN_HOURS = 1;

/** Borne max du slider horaire (créneau personnalisé) selon le type de soin */
export const AVAILABILITY_MAX_HOUR_BLOOD_TEST = 17;
export const AVAILABILITY_MAX_HOUR_NURSING = 22;
