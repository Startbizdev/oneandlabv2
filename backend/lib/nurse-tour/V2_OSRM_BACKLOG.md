# Tournée infirmier — v2 backlog

OSRM self-hosted routing is intentionally **not** implemented in v1.

v1 uses Haversine distance + nearest-neighbor ordering (zero paid geo API).
Enable OSRM only if KPI shows Haversine ordering insufficient (see nurse_tournee.plan.md phase 4).
